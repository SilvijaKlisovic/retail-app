@model IEnumerable<PrvaAplikacija.Models.Normativ>

@{
    ViewBag.Title = "Normativi proizvoda";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Proizvod.Naziv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Artikal.Naziv)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Koeficijent)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Proizvod.Naziv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Artikal.Naziv)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Koeficijent)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NormativID }) |
            @Html.ActionLink("Details", "Details", new { id=item.NormativID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NormativID })
        </td>
    </tr>
}

</table>
